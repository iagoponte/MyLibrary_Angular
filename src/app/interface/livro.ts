export interface Livro {
    id: number,
    titulo: string,
    autor: string,
    ano_publicacao: number,
    genero: string,
    capa: string,
    sinopse: string,
    preco: number, 
    created_at: string
}
